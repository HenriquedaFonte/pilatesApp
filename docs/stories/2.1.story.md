# Story 2.1 — Painel de Alunos Excluídos + Restauração

**Epic:** 2 — Recuperação (Fase 2 — SHOULD)
**Status:** Done
**Estimativa:** M (4–6h)
**Tipo:** Frontend + Backend
**Depende de:** Story 1.1, Story 1.2, Story 1.3 (completas)

---

## User Story

**Como** professora do estúdio,
**Quero** ver a lista de alunos que foram movidos para a lixeira e poder restaurá-los,
**Para que** eu possa corrigir exclusões acidentais sem perder o histórico de aulas.

---

## Contexto para o Dev

Com o soft delete implementado (Stories 1.1–1.3), os alunos excluídos existem em `profiles` com `deleted_at IS NOT NULL`. Esta story cria uma seção "Alunos na Lixeira" no `TeacherStudents.jsx` com a lista desses alunos e botão de restauração.

A restauração chama `manage-student` com `action: 'restore'` (implementado na Story 1.2).

---

## Acceptance Criteria

- [x] **AC-1:** Nova seção "Alunos na Lixeira" visível em `TeacherStudents.jsx`, colapsável (acordeon ou toggle), abaixo da lista principal
- [x] **AC-2:** A seção exibe: nome do aluno, email, data de exclusão (`deleted_at` formatada), dias restantes para hard delete automático (90 dias a partir de `deleted_at`)
- [x] **AC-3:** Botão "Restaurar" por aluno — ao clicar, abre `AlertDialog` de confirmação
- [x] **AC-4:** Ao confirmar restauração, invoca `manage-student` com `{ action: 'restore', studentId }` e exibe mensagem de sucesso
- [x] **AC-5:** Após restauração bem-sucedida, aluno some da lixeira e reaparece na lista principal
- [x] **AC-6:** Se não houver alunos na lixeira, a seção exibe mensagem: "Nenhum aluno na lixeira"
- [x] **AC-7:** Badge de contagem na seção: "Lixeira (N)" onde N é o número de alunos em soft delete

---

## Escopo

### IN
- Nova seção colapsável "Alunos na Lixeira" em `TeacherStudents.jsx`
- Query separada para buscar `profiles` com `deleted_at IS NOT NULL`
- Handler `handleRestoreStudent` que chama `action: 'restore'`
- Cálculo de dias restantes até hard delete automático (90 dias)
- AlertDialog de confirmação de restauração

### OUT
- Hard delete manual (botão "Excluir Permanentemente") → Story 2.2
- Hard delete automático via cron → Epic 3 (futuro)
- Painel de auditoria (visualizar audit_log) → fora do escopo desta story

---

## Riscos

| Risco | Probabilidade | Impacto | Mitigação |
|-------|---------------|---------|-----------|
| Fetch duplo (ativos + lixeira) degrada performance | Baixa | Baixo | Supabase Free suporta bem — tabela `profiles` tem poucos registros; otimizar se necessário |
| Restaurar aluno cujo email já existe em outro perfil ativo | Baixa | Alto | `manage-student action: 'restore'` deve verificar unicidade de email antes de restaurar (tratar na Story 1.2 edge case) |
| Hard delete automático (90 dias) executar antes da UI estar pronta | N/A | Alto | Hard delete automático está no Epic 3 (futuro) — não há cron configurado ainda |
| Aluno restaurado sem `student_class_link` (perdeu as turmas) | Alta | Médio | Expected — turmas eram removidas no soft delete (Story 1.2 AC-4); professora deve rematricular manualmente |

---

## Dev Notes

### Arquivo Principal
- `src/pages/TeacherStudents.jsx`

### Query para Alunos na Lixeira
```javascript
const fetchDeletedStudents = async () => {
  const { data, error } = await supabase
    .from('profiles')
    .select('id, email, full_name, deleted_at, deleted_by')
    .eq('role', 'student')
    .not('deleted_at', 'is', null)
    .order('deleted_at', { ascending: false })
  
  if (!error) setDeletedStudents(data || [])
}
```

### Cálculo de Dias Restantes
```javascript
const getDaysUntilHardDelete = (deletedAt) => {
  const deleteDate = new Date(deletedAt)
  const hardDeleteDate = new Date(deleteDate)
  hardDeleteDate.setDate(hardDeleteDate.getDate() + 90)
  const today = new Date()
  const diffMs = hardDeleteDate - today
  return Math.max(0, Math.ceil(diffMs / (1000 * 60 * 60 * 24)))
}
```

### Handler de Restauração
```javascript
const handleRestoreStudent = async () => {
  try {
    const { data, error } = await supabase.functions.invoke('manage-student', {
      body: { action: 'restore', studentId: selectedStudent.id }
    })
    if (error) throw new Error(error.message)
    showSuccess(`Aluno(a) ${selectedStudent.full_name} restaurado(a) com sucesso!`)
    await fetchData()
    await fetchDeletedStudents()
  } catch (err) {
    showError('Erro ao restaurar aluno: ' + err.message)
  } finally {
    setIsRestoreConfirmOpen(false)
    setSelectedStudent(null)
  }
}
```

### Novos Estados React necessários
```javascript
const [deletedStudents, setDeletedStudents] = useState([])
const [isRestoreConfirmOpen, setIsRestoreConfirmOpen] = useState(false)
const [showDeletedSection, setShowDeletedSection] = useState(false)
```

### Ícones Disponíveis (já importados)
- `Trash2` — ícone da lixeira (importado na linha 53)
- `UserCheck` — ícone de restauração (importado na linha 51)
Podem ser necessários: `ChevronDown`, `ChevronUp` para o toggle da seção

### Estilo da Seção (consistente com o resto do componente)
```jsx
<div className="mt-8 border-t border-border/50 pt-6">
  <button
    onClick={() => setShowDeletedSection(!showDeletedSection)}
    className="flex items-center gap-2 text-sm font-semibold text-muted-foreground hover:text-foreground"
  >
    <Trash2 className="h-4 w-4" />
    Lixeira ({deletedStudents.length})
    {showDeletedSection ? <ChevronUp /> : <ChevronDown />}
  </button>
  
  {showDeletedSection && (
    <div className="mt-4 space-y-3">
      {deletedStudents.length === 0 ? (
        <p className="text-sm text-muted-foreground italic">Nenhum aluno na lixeira.</p>
      ) : (
        deletedStudents.map(student => (
          <div key={student.id} className="flex items-center justify-between p-3 bg-muted/20 rounded-xl border border-border/40">
            <div>
              <p className="text-sm font-semibold">{student.full_name}</p>
              <p className="text-xs text-muted-foreground">{student.email}</p>
              <p className="text-xs text-amber-600 mt-0.5">
                Excluído em {new Date(student.deleted_at).toLocaleDateString()} •{' '}
                {getDaysUntilHardDelete(student.deleted_at)} dias restantes
              </p>
            </div>
            <Button
              size="sm"
              variant="outline"
              onClick={() => { setSelectedStudent(student); setIsRestoreConfirmOpen(true) }}
              className="rounded-lg h-8 px-3 text-xs text-emerald-600 border-emerald-200 hover:bg-emerald-50"
            >
              <UserCheck className="h-3.5 w-3.5 mr-1" />
              Restaurar
            </Button>
          </div>
        ))
      )}
    </div>
  )}
</div>
```

---

## Tasks / Subtasks

- [x] **Task 1 (AC-2, AC-6, AC-7):** Adicionar estado `deletedStudents` e função `fetchDeletedStudents` ao componente

- [x] **Task 2:** Chamar `fetchDeletedStudents` dentro de `fetchData` (ou separadamente no `useEffect`)

- [x] **Task 3 (AC-1):** Criar seção "Lixeira" colapsável abaixo da grid de alunos ativos

- [x] **Task 4 (AC-2):** Implementar `getDaysUntilHardDelete` e exibir na linha de cada aluno excluído

- [x] **Task 5 (AC-3, AC-4):** Adicionar handler `handleRestoreStudent` e `AlertDialog` de confirmação de restauração

- [x] **Task 6 (AC-5):** Após restauração, chamar `fetchData()` e `fetchDeletedStudents()` para atualizar ambas as listas

- [x] **Task 7:** Testar fluxo completo no browser:
  - Aluno aparece na lixeira com contagem de dias
  - Clicar "Restaurar" → modal de confirmação
  - Confirmar → aluno volta para lista principal, some da lixeira

---

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- Primary Type: Frontend
- Secondary Type: API (chamada a `action: 'restore'`)
- Complexity: Medium

**Specialized Agent Assignment:**
- @dev (implementação)
- @ux-expert (validação UX — comunicação de dias restantes)

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): Verificar que `fetchDeletedStudents` usa `.not('deleted_at', 'is', null)`
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main`

**CodeRabbit Focus Areas:**
- Query separation: alunos ativos e deletados em queries separadas
- Cálculo de dias: usar `Math.max(0, ...)` para evitar números negativos
- Refresh após restauração: ambas as listas atualizadas

**Self-Healing:**
- Primary Agent: @dev (light mode, max 2 iter, CRITICAL only)

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-06-17 | @sm (River) | Story criada (Draft) — PRD-001 Fase 2 |
| 2026-06-17 | @po (Pax) | Validação GO (9/10) — Status: Draft → Ready |
| 2026-06-17 | @dev (Dex) | Implementação iniciada — Status: Ready → InProgress |
| 2026-06-17 | @dev (Dex) | Implementação concluída — Status: InProgress → InReview |
| 2026-06-17 | @dev (Dex) | Bug fix: `handleDeleteStudent` não chamava `fetchDeletedStudents()` — lixeira não atualizava sem F5. Corrigido. |
| 2026-06-17 | @dev (Dex) | Task 7 verificada no browser — todos os ACs confirmados |
