# Story 2.2 — Hard Delete com Fricção Intencional

**Epic:** 2 — Recuperação (Fase 2 — SHOULD)
**Status:** InReview
**Estimativa:** S (2–3h)
**Tipo:** Frontend
**Depende de:** Story 2.1 (painel de lixeira implementado)

---

## User Story

**Como** professora do estúdio,
**Quero** que a exclusão permanente de um aluno exija que eu digite uma frase de confirmação,
**Para que** exclusões permanentes acidentais sejam praticamente impossíveis.

---

## Contexto para o Dev

Com soft delete e painel de recuperação em vigor (Stories 1.1–2.1), o hard delete permanente é uma operação deliberada que deve ser difícil de executar acidentalmente. Esta story adiciona um campo de texto no modal de confirmação que exige digitar `EXCLUIR PERMANENTEMENTE` antes de habilitar o botão de ação.

O hard delete é acessado a partir da lixeira (Story 2.1), não da lista principal de alunos ativos.

---

## Acceptance Criteria

- [x] **AC-1:** O botão "Excluir Permanentemente" aparece apenas no painel da lixeira (Story 2.1), não na lista de alunos ativos
- [x] **AC-2:** Ao clicar "Excluir Permanentemente", abre um `AlertDialog` especial com campo de texto
- [x] **AC-3:** O campo de texto exibe placeholder: `Digite EXCLUIR PERMANENTEMENTE para confirmar`
- [x] **AC-4:** O botão de confirmação permanece **desabilitado** até o campo conter exatamente `EXCLUIR PERMANENTEMENTE` (case-sensitive)
- [x] **AC-5:** O modal exibe aviso explícito: "Esta ação é irreversível. Histórico de aulas, créditos e dados do aluno serão perdidos para sempre."
- [x] **AC-6:** Ao confirmar, invoca `manage-student` com `{ action: 'hard-delete', studentId }` e registra no audit_log (responsabilidade da edge function — Story 1.2)
- [x] **AC-7:** Após hard delete bem-sucedido, aluno some da lixeira e mensagem de sucesso é exibida

---

## Escopo

### IN
- Botão "Excluir Permanentemente" nos cards da lixeira (Story 2.1)
- `AlertDialog` com campo de texto de confirmação
- Validação client-side do campo (case-sensitive)
- Handler `handleHardDeleteStudent`

### OUT
- Hard delete automático via cron (90 dias) → Epic 3 (futuro)
- Mudanças na edge function → já implementado na Story 1.2 (`action: 'hard-delete'`)

---

## Riscos

| Risco | Probabilidade | Impacto | Mitigação |
|-------|---------------|---------|-----------|
| Autocorrect do celular capitaliza/modifica a frase de confirmação | Média | Médio | Task 3 inclui `autoCapitalize="none"` e `autoCorrect="off"` no `Input` de confirmação |
| Campo de confirmação não limpa ao fechar modal | Alta | Baixo | `onOpenChange` limpa `hardDeleteConfirmText` ao fechar — AC-4 e Task 4 cobrem isso |
| Botão de hard delete exposto na lista de alunos ativos | Baixa | Alto | AC-1 explícito: botão aparece apenas nos cards da lixeira (Story 2.1) |
| Hard delete executado sem audit log registrado | Baixa | Alto | Edge function `action: 'hard-delete'` (Story 1.2) registra audit ANTES da exclusão |

---

## Dev Notes

### Arquivo Principal
- `src/pages/TeacherStudents.jsx`

### Novos Estados React
```javascript
const [isHardDeleteConfirmOpen, setIsHardDeleteConfirmOpen] = useState(false)
const [hardDeleteConfirmText, setHardDeleteConfirmText] = useState('')
const [hardDeleteLoading, setHardDeleteLoading] = useState(false)

const HARD_DELETE_PHRASE = 'EXCLUIR PERMANENTEMENTE'
```

### Handler de Hard Delete
```javascript
const handleHardDeleteStudent = async () => {
  if (hardDeleteConfirmText !== HARD_DELETE_PHRASE) return
  
  setHardDeleteLoading(true)
  try {
    const { data, error } = await supabase.functions.invoke('manage-student', {
      body: { action: 'hard-delete', studentId: selectedStudent.id }
    })
    if (error) throw new Error(error.message)
    showSuccess(`Aluno(a) ${selectedStudent.full_name} excluído(a) permanentemente.`)
    await fetchDeletedStudents()
  } catch (err) {
    showError('Erro ao excluir permanentemente: ' + err.message)
  } finally {
    setIsHardDeleteConfirmOpen(false)
    setHardDeleteConfirmText('')
    setSelectedStudent(null)
    setHardDeleteLoading(false)
  }
}
```

### Modal de Confirmação com Fricção
```jsx
<AlertDialog open={isHardDeleteConfirmOpen} onOpenChange={(open) => {
  setIsHardDeleteConfirmOpen(open)
  if (!open) setHardDeleteConfirmText('')
}}>
  <AlertDialogContent className="rounded-2xl">
    <AlertDialogHeader>
      <AlertDialogTitle className="text-base font-bold text-destructive">
        ⚠️ Exclusão Permanente
      </AlertDialogTitle>
      <AlertDialogDescription className="text-xs text-muted-foreground space-y-2">
        <p>
          Você está prestes a excluir permanentemente o(a) aluno(a){' '}
          <strong>{selectedStudent?.full_name}</strong>.
        </p>
        <p className="text-destructive font-semibold">
          Esta ação é irreversível. Histórico de aulas, créditos e dados do aluno serão perdidos para sempre.
        </p>
        <p>Digite <code className="bg-muted px-1 rounded text-foreground font-mono">EXCLUIR PERMANENTEMENTE</code> para confirmar:</p>
      </AlertDialogDescription>
    </AlertDialogHeader>
    <div className="px-1 pb-2">
      <Input
        value={hardDeleteConfirmText}
        onChange={e => setHardDeleteConfirmText(e.target.value)}
        placeholder="Digite EXCLUIR PERMANENTEMENTE para confirmar"
        className="rounded-xl border-destructive/50 focus-visible:ring-destructive h-10 font-mono text-sm"
        autoFocus
      />
    </div>
    <AlertDialogFooter>
      <AlertDialogCancel className="rounded-xl" onClick={() => {
        setIsHardDeleteConfirmOpen(false)
        setHardDeleteConfirmText('')
        setSelectedStudent(null)
      }}>
        Cancelar
      </AlertDialogCancel>
      <AlertDialogAction
        className="rounded-xl bg-destructive hover:bg-destructive/90 text-white disabled:opacity-40"
        onClick={handleHardDeleteStudent}
        disabled={hardDeleteConfirmText !== HARD_DELETE_PHRASE || hardDeleteLoading}
      >
        {hardDeleteLoading ? 'Excluindo...' : 'Excluir Permanentemente'}
      </AlertDialogAction>
    </AlertDialogFooter>
  </AlertDialogContent>
</AlertDialog>
```

### Botão na Lixeira (Story 2.1 — adicionar ao card de aluno excluído)
```jsx
<Button
  size="sm"
  variant="outline"
  onClick={() => { setSelectedStudent(student); setIsHardDeleteConfirmOpen(true) }}
  className="rounded-lg h-8 px-3 text-xs text-destructive border-destructive/30 hover:bg-destructive/5"
>
  <Trash2 className="h-3.5 w-3.5 mr-1" />
  Excluir Para Sempre
</Button>
```

### Imports Adicionais Necessários
- `Input` já importado (linha 26)
- `AlertDialog`, `AlertDialogAction`, etc. já importados (linhas 68–74)
- `Trash2` já importado (linha 52)

---

## Tasks / Subtasks

- [x] **Task 1 (AC-1):** Adicionar botão "Excluir Para Sempre" nos cards da seção lixeira (Story 2.1), NÃO na lista de alunos ativos

- [x] **Task 2:** Adicionar estados: `isHardDeleteConfirmOpen`, `hardDeleteConfirmText`, `hardDeleteLoading`

- [x] **Task 3 (AC-2, AC-3, AC-4, AC-5):** Criar `AlertDialog` com campo de texto de confirmação
  - Placeholder correto
  - Botão desabilitado até frase correta
  - Aviso de irreversibilidade destacado
  - Adicionar `autoCapitalize="none"` e `autoCorrect="off"` no `Input` (mitigação risco de autocorrect)

- [x] **Task 4 (AC-4):** Limpar campo ao fechar o modal (no `onOpenChange`)

- [x] **Task 5 (AC-6, AC-7):** Implementar `handleHardDeleteStudent` que chama `action: 'hard-delete'`

- [x] **Task 6:** Testar fluxo completo no browser:
  - Abrir lixeira → ver botão "Excluir Para Sempre"
  - Clicar → modal abre com campo vazio, botão desabilitado
  - Digitar frase errada → botão permanece desabilitado
  - Digitar `EXCLUIR PERMANENTEMENTE` → botão ativa
  - Confirmar → aluno some da lixeira, mensagem de sucesso

---

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- Primary Type: Frontend
- Secondary Type: Security (fricção intencional)
- Complexity: Low

**Specialized Agent Assignment:**
- @dev (implementação)

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): Verificar que botão permanece desabilitado quando `text !== 'EXCLUIR PERMANENTEMENTE'`
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main`

**CodeRabbit Focus Areas:**
- Case-sensitivity: comparação `===` não `toLowerCase()`
- Limpeza do campo: `setHardDeleteConfirmText('')` ao fechar modal
- Botão exposto apenas na lixeira: confirmar que não aparece na lista de ativos

**Self-Healing:**
- Primary Agent: @dev (light mode, max 2 iter, CRITICAL only)

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-06-17 | @sm (River) | Story criada (Draft) — PRD-001 Fase 2 |
| 2026-06-17 | @po (Pax) | Validação GO (9/10) — Status: Draft → Ready |
| 2026-06-17 | @po (Pax) | Adicionada seção formal de Riscos + mitigação autocorrect no Task 3 — 10/10 |
| 2026-06-17 | @dev (Dex) | Implementação iniciada — Status: Ready → InProgress |
| 2026-06-17 | @dev (Dex) | Implementação concluída — Status: InProgress → InReview |
| 2026-06-17 | @dev (Dex) | Task 6 verificada no browser — todos os ACs confirmados (fricção funcional, hard delete executado com sucesso) |
